import { connect } from "react-redux";
import ReviewOnTheAppStore from "./reviewontheappstore";

const mapStateToProps = () => ({});

const mapDispatchToProps = () => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(ReviewOnTheAppStore);
