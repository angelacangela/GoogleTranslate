import { connect } from "react-redux";
import SpeechRegion from "./speechRegion";

const mapStateToProps = () => ({});

const mapDispatchToProps = () => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(SpeechRegion);
