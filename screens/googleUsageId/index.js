import { connect } from "react-redux";
import GoogleUsageId from "./googleUsageId";

const mapStateToProps = () => ({});

const mapDispatchToProps = () => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(GoogleUsageId);
