import { connect } from "react-redux";
import ContentLicenses from "./ContentLicenses";

const mapStateToProps = () => ({});

const mapDispatchToProps = () => ({});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(ContentLicenses);
